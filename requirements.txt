# ── Web Framework ────────────────────────────────────────────
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6

# ── Data Processing ─────────────────────────────────────────
duckdb==0.9.2
pyarrow==14.0.1
pandas==2.1.3
numpy==1.24.3

# ── Embeddings ──────────────────────────────────────────────
sentence-transformers==2.7.0
# transformers pinned to <4.36.0: versions 4.36+ use torch.utils._pytree.register_pytree_node
# which was removed in torch 2.1.x (see: https://github.com/huggingface/transformers/issues/27392)
transformers==4.35.2
torch==2.1.1

# ── TTS (Text-to-Speech) ───────────────────────────────────
# Default engine (cloud, free)
edge-tts==6.1.9

# Optional TTS engines – uncomment to enable:
# Coqui / XTTS v2 / FastPitch / Tortoise (shared package)
# TTS>=0.22.0
# Bark (expressive, multilingual)
# git+https://github.com/suno-ai/bark.git
# Required by Bark for WAV output
# scipy>=1.11.0

# ── LLM Integration ────────────────────────────────────────
httpx==0.25.2

# ── Document Parsing ───────────────────────────────────────
PyPDF2==3.0.1
python-docx==0.8.11

# ── Audio Processing (optional, for format conversion) ─────
pydub==0.25.1
mutagen==1.47.0

# ── Environment ────────────────────────────────────────────
python-dotenv==1.0.0

# ── Testing ────────────────────────────────────────────────
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-html==4.1.1
pytest-cov==4.1.0

# ── Code Quality ───────────────────────────────────────────
black==23.12.0
flake8==6.1.0
